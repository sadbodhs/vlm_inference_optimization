# E7c primary detector: YOLOv8s, the same weights as E7/E7b, exported with
# DeepStream-Yolo's export_yoloV8.py at 1280 with a dynamic batch.
# Matches the ultralytics path where it matters: letterboxed (maintain aspect,
# symmetric padding), conf 0.25, NMS IoU 0.7 (ultralytics' default -- the
# DeepStream-Yolo template ships 0.45, which would suppress more boxes).
[property]
gpu-id=0
net-scale-factor=0.0039215697906911373
model-color-format=0
onnx-file=/models/yolov8s.onnx
# DeepStream-Yolo's engine builder writes model_b<N>_gpu0_fp16.engine to the
# working directory, so that is where it is looked for
model-engine-file=/work/model_b16_gpu0_fp16.engine
labelfile-path=/models/labels.txt
batch-size=16
network-mode=2
num-detected-classes=80
# detect every 6th frame (5 fps at 30 fps), the tracker covers the frames between
interval=5
gie-unique-id=1
process-mode=1
network-type=0
cluster-mode=2
maintain-aspect-ratio=1
symmetric-padding=1
workspace-size=2048
parse-bbox-func-name=NvDsInferParseYolo
custom-lib-path=/opt/ds/DeepStream-Yolo/nvdsinfer_custom_impl_Yolo/libnvdsinfer_custom_impl_Yolo.so
engine-create-func-name=NvDsInferYoloCudaEngineGet

[class-attrs-all]
nms-iou-threshold=0.7
pre-cluster-threshold=0.25
topk=300
