# E7d (PLAN.md 13): Qwen3.5 2B, 4-bit, in the cascade. Everything but the
# model is V_vllm_video, so a difference against it is the model.
id: E_q35_2b
stack: vllm
model: cyankiwi/Qwen3.5-2B-AWQ-4bit
revision: 718fd9b52c5ca16f1f856f6c4e4209129802a6a2
base_url: http://vlm-server:8000
quantization: compressed-tensors

# safetensors on disk; the vision/LLM split is filled from the headers after download
weight_bytes: 2.501e+9

max_tokens: 16
temperature: 0.0
max_pixels: null          # per-frame cap applied client-side, as in E7

max_images: 16
max_model_len: 16384

server_args: --no-enable-prefix-caching --no-enable-chunked-prefill --mm-processor-cache-gb 0

# Qwen3.5-4B/9B templates think unless told not to; a 16-token answer would be
# cut off mid-thought. Off for every Qwen3.5 arm, so the three are comparable.
extra_body:
  chat_template_kwargs:
    enable_thinking: false

notes: >
  Qwen3.5 2B. Compare against V_vllm_video (Qwen2.5-VL-7B-AWQ) in E7/E7c.
